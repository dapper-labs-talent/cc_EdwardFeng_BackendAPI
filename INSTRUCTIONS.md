### Setup

- have `node` and `npm` installed. Run `npm i`.
- Run `npm run build && npm run server:prod` to run the prod server locally

### Potential Problem

- Since I host my DB on MongoDB Cloud, the security measure might block any additional connection from different IP than mine.
